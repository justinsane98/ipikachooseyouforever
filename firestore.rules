rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sets/base1/cards/{card} {
      allow get;
      allow read;
    }

     match /leaderboard/{record} {
      allow get;
      allow read;
      allow create;
    }
    
  }
}